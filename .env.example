# Optional for local development; required in production.
OPENINQUIRY_APP_ORIGIN=http://localhost:3000

# Required in production. Supply at least 32 random bytes through the hosting
# platform's secret manager. This template intentionally contains no secret.
OPENINQUIRY_SESSION_SECRET=
